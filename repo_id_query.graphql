query RepoId($owner: String!, $name: String!) {
  repository(owner: $owner, name: $name) {
    id
    owner {
      __typename
      login
    }
    name
    latestRelease
    url
    parent {
      name
      owner {
        __typename
        login
      }
    }
  }
}
